# ProductMatchPro - Hydraulic Product Cross-Reference RAG Application
# Install: pip install -r requirements.txt

# LLM Providers (dual-provider: set LLM_PROVIDER=anthropic or openai in .env)
anthropic>=0.40.0
openai>=1.30.0

# PDF parsing — PyMuPDF is the primary extractor (best for complex layouts,
# multi-column text, operating data tables). pypdf is a lightweight fallback.
# pdfplumber is used for structured table extraction and supplementary text.
pymupdf>=1.24.0
pypdf>=5.4.0
pdfplumber>=0.10.0

# Vector store & embeddings (numpy-based, no ChromaDB for Python 3.14 compat)
numpy>=1.26.0
sentence-transformers>=2.3.0

# Fuzzy matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# Data handling
pydantic>=2.0.0
pandas>=2.0.0

# UI
gradio>=5.0.0

# Utilities
python-dotenv>=1.0.0
